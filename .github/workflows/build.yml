name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'
          channel: 'stable'
          cache: true

      - name: Download source zip
        run: |
          curl -L -o src.zip "https://litter.catbox.moe/p93tys.zip"
          mkdir -p project
          unzip -o src.zip -d project
          if [ -f project/pubspec.yaml ]; then echo Flat; else DIR=$(find project -name pubspec.yaml -exec dirname {} \; | head -1); cp -a "$DIR"/. project/; fi
          # Keep AGP 8.3.2 pinned in settings; force newDsl=false
          grep -q 'android.newDsl=false' project/android/gradle.properties || echo 'android.newDsl=false' >> project/android/gradle.properties
          # Prevent Flutter from upgrading AGP in settings
          sed -i 's/id("com.android.application") version ".*"/id("com.android.application") version "8.3.2"/' project/android/settings.gradle.kts || true
          sed -i 's/id("org.jetbrains.kotlin.android") version ".*"/id("org.jetbrains.kotlin.android") version "1.9.23"/' project/android/settings.gradle.kts || true
          ls project/
          cat project/android/settings.gradle.kts
          cat project/android/gradle.properties

      - name: Get dependencies
        working-directory: project
        run: flutter pub get

      - name: Build APK
        working-directory: project
        run: |
          flutter build apk --release
          find build -name "*.apk" -type f || true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: powerfiles-apk-v5
          path: project/build/app/outputs/flutter-apk/*.apk
          if-no-files-found: warn
